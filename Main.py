import field_coords

# Example usage
field_coords.startPos(0, 0, 0)
field_coords.move_to_pivot_forward(1, 1)
field_coords.move_to_tank_forward(2, 2)
field_coords.move_to_pivot_backward(1, 0)
field_coords.move_to_tank_backward(0, 0)
field_coords.end()
